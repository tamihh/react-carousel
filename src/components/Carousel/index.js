import React from 'react';
import PropTypes from 'prop-types';

const Carousel = props => {
  return (
    <div>
      carousel
    </div>
  );
};

Carousel.propTypes = {
  
};

export default Carousel;